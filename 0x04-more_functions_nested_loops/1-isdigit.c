#include "main.h"
/**
 *_isdigit -  check for a code tha checks for 0 to 9
  @ c: *inter to be checked
 * Return:1 if c=digit or else 0 otherwise
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
