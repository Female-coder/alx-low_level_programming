#include <stdio.h>
/**
 * main - prints its functions name
 * @argc: the argument counter for char 
 * @argv: the  argument value for char
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
